services:
  - type: web
    name: netguard-pro
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn netguard:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
